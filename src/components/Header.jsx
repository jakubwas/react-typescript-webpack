import React from "react";
import { StyledHeaderContainer, StyledHeader } from "./Header.styles";

const Header = () => {
  return (
    <StyledHeaderContainer>
      <StyledHeader>React App with Webpack Config</StyledHeader>
    </StyledHeaderContainer>
  );
};

export default Header;
